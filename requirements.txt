# Core Streamlit dependencies
streamlit
numpy
pandas
pillow

# 🚨 THE CRITICAL FIX: Use the stable PyTorch index URL 
# This ensures a CPU-only version is installed reliably.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision